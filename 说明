

# 📘 SuperTxt 产品与技术功能详细说明书 (v1.0)

## 第一部分：产品概述与定位 (Product Overview)

### 1.1 产品愿景

在大语言模型（LLM）普及的时代，知识工作者每天需要进行海量的“复制、提纯、拼接、喂给 AI”的文本操作。传统的云笔记（如 Notion）太重且排版容易干扰代码；传统的 Windows 记事本（Notepad）又缺乏基本的结构化管理和富文本展示能力。
**SuperTxt** 旨在填补这一空白。它被定义为 **“AI 时代 Windows 记事本的终极形态”**——一款基于本地文件系统（Local-First）、极速秒开、支持 TXT 与 Markdown 智能双模、且深度融合大模型上下文工作流的轻量级个人知识库（PKM）工具。

### 1.2 目标用户群体

1. **提示词工程师 & AI 极客**：需要频繁搬运、存储、提纯 AI 对话上下文（Context）的用户。
2. **开发者 & 程序员**：需要一款能极速打开、支持代码块高亮、且完全由本地硬盘管理的文档工具。
3. **日常记录者**：需要替代 Windows 系统自带记事本，拥有更好目录管理和待办事项（Todo）功能的轻度用户。

### 1.3 技术架构选型

* **桌面端底座**：Tauri (使用 OS 原生 WebView2，摒弃 Chromium 以实现极致轻量化，安装包 < 10MB)。
* **前端框架**：React 18 + TailwindCSS (提供高响应的现代用户界面)。
* **底层通信**：Rust (负责文件 I/O、系统截图唤起、二进制流处理)。
* **数据存储**：完全基于物理硬盘（直读直写 `.txt` 和 `.md` 文件），辅以 `supertxt_index.json` 作为历史访问和折叠状态的轻量级缓存。

---

## 第二部分：全局 UI 与交互规范 (Global UI & UX)

### 2.1 整体布局规划

系统采用经典的 **“三栏式响应布局 (Three-column Layout)”**，从左至右依次为：

1. **侧边栏 (Sidebar)**：宽度支持拖拽动态调整（160px - 400px）。包含项目名称、深/浅色模式切换、全部文件入口、最近访问列表、目录树、标签面板以及全局设置。
2. **中间栏 (File List)**：默认宽 288px，支持拖拽动态调整（200px - 400px）。资源管理器级的文件列表，包含全局搜索框、新建按钮及当前选中目录的直属文件列表。
3. **主工作区 (Editor Workspace)**：自适应宽度。顶部为多标签页 (Tabs) 栏和富功能工具栏，主体为沉浸式编辑器，支持大纲目录面板侧滑。

### 2.2 沉浸式与视觉模式

* **暗黑模式 (Dark Mode)**：提供全局的 Light/Dark 主题切换，完美适配环境光线。
* **独立沉浸模式 (Zen Mode)**：点击工具栏的“最大化”按钮，左侧两栏将通过 CSS 动画完全隐藏，编辑器区域居中全屏，提供无干扰的极致写作心流。

### 2.3 异常处理与容错机制

* **全局错误边界 (ErrorBoundary)**：应用最外层被 React 错误边界包裹。当发生严重的 JavaScript 运行时异常时，系统将拦截崩溃，不出现“白屏”，而是展示红色的故障诊断面板及重试按钮。
* **非阻塞通知 (Toast)**：废弃所有原生的 `alert` 弹窗。系统操作（如保存成功、非法移动、同名拦截）均通过页面中上方弹出的平滑动画 Toast 气泡进行提示，3 秒后自动消失。
* **模态对话框 (Modal)**：所有二次确认、移动文件、历史记录查看，均使用带有背景高斯模糊（backdrop-blur）的居中弹出层。

---

## 第三部分：核心功能模块详述 (Core Features)

### 3.1 智能目录树与资源管理系统 (Directory System)

本系统不依赖封闭数据库，左侧目录树是本地物理文件夹的绝对映射。

#### 3.1.1 四色状态可视化目录

为了让用户在不展开目录的情况下掌握信息，文件夹图标采用了四种状态颜色自动计算渲染：

1. **蓝色 (Blue)**：**根目录**级别（最外层），带有半透明底色，突显主干。
2. **浅灰色 (Gray)**：**空目录**。内部既无文件也无子文件夹，一眼可辨。
3. **橙黄色 (Orange)**：**仅含子目录**。内部没有直接的笔记文件，但存在更深层的分类。
4. **翠绿色 (Green)**：**包含文件**。只要该目录下有实质性的笔记文件，即高亮为绿色，代表“知识节点”。

#### 3.1.2 像素级对齐与直属渲染

* **严格层级对齐**：同一目录层级下的“文件夹”与“文件”，在视觉上做到左侧 100% 垂直对齐。针对文件夹独有的展开箭头，文件通过精准的 `w-4` (16px) 透明占位符进行缩进补偿。
* **直属文件展示逻辑**：点击左侧任意目录，右侧的中间栏列表**仅展示直接归属于该目录的文件**，不再混合递归子目录的内容，严格遵循 Windows 资源管理器的逻辑。

#### 3.1.3 目录操作与上下文菜单 (Context Menu)

在目录上点击鼠标右键，唤出系统级自定义菜单：

* **新建子目录**：在当前层级动态生成输入框，支持回车确认，`Esc` 取消。
* **移动该目录**：弹出移动模态框，用户可选择将其移动到其他父目录下。系统包含**防死锁拦截**（禁止将父目录移入自己的子目录下）。
* **删除目录**：系统会递归检查。**若目录下存在任何文件或子目录，系统将强制阻断删除并 Toast 警告**，确保用户数据绝对安全；仅允许删除空目录。

### 3.2 笔记列表与搜索系统 (List & Search)

#### 3.2.1 极速检索与多重视图

* **全局搜索**：中间栏顶部提供搜索框。支持基于“标题”和“正文内容”的实时模糊检索（基于 `useMemo` 计算，输入即出结果）。
* **全部文件视图**：点击左侧“全部文件”，中间栏将无视目录层级，平铺展示系统内的所有笔记。
* **最近访问 (History)**：左侧边栏提供最多 5 条最近操作历史，显示精确的修改时间（“今天 09:30”或“05-12”）。点击下方“查看全部”，弹出独立的瀑布流历史访问大屏，支持分页查看海量历史。

#### 3.2.2 列表项的富展示

每个笔记卡片包含：

* **标题与摘要**：自动剥离 Markdown 语法，显示纯净的前两行正文作为预览。
* **格式角标**：以醒目的样式标识该文件是 `MD`（紫色背景）还是 `TXT`（灰色背景）。
* **更新时间**：精确到天的最后修改时间。
* **右键菜单**：支持在列表项上右键，快速执行“移动笔记”、“格式转换”和“物理删除”操作。

### 3.3 顶部多标签页管理 (Multi-Tab Management)

为了满足并行任务（如一边看架构文档，一边写大模型 Prompt），系统引入了浏览器级的多标签架构：

* **动态添加**：点击任意笔记，若未打开则追加至标签栏；若已打开则平滑切换焦点。
* **水平溢出滚动**：标签数量超出屏幕宽度时，支持横向滚动条以及鼠标滚轮滑动。
* **焦点回退算法**：当用户点击 `X` 关闭当前处于激活状态的标签时，系统会自动将焦点平滑回退到剩余标签列表的最后一个，避免出现无内容的空白状态。
* **一键清理**：当打开标签数 ≥ 2 个时，标签栏最右侧的遮罩层上方会悬浮显示“全部关闭”快捷键。

### 3.4 核心编辑器引擎：TXT与MD的无缝穿梭

这是 SuperTxt 的灵魂模块。应用提供极度纯粹的 `.txt` 体验，又能瞬间切换为专业的 `.md` 生产力环境。

#### 3.4.1 TXT 纯净模式

* 在此模式下，工具栏的所有富文本排版按钮（如加粗、列表）将全部保留显示，但左侧会显示一个醒目的“TXT 纯净模式”标签。
* **同名检测与自动存盘**：修改标题时，系统实时检测当前目录是否存在同名物理文件并拦截。停止打字 800 毫秒后，自动通过 Rust 写入磁盘。右上角显示“保存中...”至“已同步”的呼吸动画。

#### 3.4.2 智能升级转化 (Smart Upgrade)

* 用户在 TXT 模式下，如果点击了工具栏的“加粗”、“代办模板”等排版按钮，系统会**智能拦截**该操作。
* 弹出精美的升级确认对话框：“TXT 格式无法保存富文本样式，是否升级为 Markdown？”
* 用户同意后，系统在底层执行：修改文件内存后缀 -> 追加所选的 Markdown 排版语法 -> 删除旧的 TXT，这一切在不到 0.1 秒内完成。

#### 3.4.3 Markdown 双视图 (源码 vs 视觉)

* **源码模式 (Source)**：直接编辑纯文本，支持快捷工具插入（如点击“加粗”会在光标两侧插入 `` 符号并居中焦点）。
* **视觉模式 (Visual/WYSIWYG)**：
* 采用 `contentEditable` 底层引擎。
* 用户可以像在 Microsoft Word 中一样直接打字、调整排版。
* **逆向清洗引擎**：当用户从视觉模式切回源码时，系统会调用手写的 `htmlToMarkdown` 引擎，将所有 HTML DOM 标签剥离、还原为干净的 Markdown 符号，确保数据不被前端冗余标签污染。



### 3.5 富排版工具栏与大模型增强 (Advanced Toolbar)

工具栏汇聚了知识管理和 AI 辅助的核心武器：

1. **基础排版**：粗体、斜体、引用 (Quote)。
2. **颜色修改 (Color Palette)**：视觉模式下点击调色板 🎨，可通过浏览器原生能力直接改变选中文字的颜色（支持 `red` 或 Hex 色值）。
3. **Todo 与时间戳**：
* 提供单行 `[ ]` 复选框插入。
* 提供“生成今日待办”快捷按钮，一键插入带当前日期的标准化待办框架。
* 一键插入精确到分钟的时间戳日志。


4. **大纲导航树 (TOC - Table of Contents)**：
* 点击工具栏最右侧的 `ListTree` 按钮，编辑器右侧会平滑推出大纲面板。
* 系统使用正则表达式，实时提取正文中的 H1 (`#`) 到 H6 (`######`) 标题。
* 生成层级缩进的大纲树，未来可扩展为点击锚点直接跳转，适合阅读长篇 AI 生成文档。



### 3.6 全局截图与图像处理黑科技 (Media & Image)

处理本地图片是纯前端 WebView 的难点，SuperTxt 通过前后端协作实现了突破：

#### 3.6.1 强制唤起系统截图

* 点击工具栏相机 📷 图标，前端触发 `invoke`。
* Rust 后端接管，首先将主程序窗口**最小化**。
* 唤起 Windows 原生 `ms-screenclip:` 工具（兼容 Win10/Win11），允许用户框选屏幕。

#### 3.6.2 二进制流读写与越狱渲染

* **全局拦截**：用户在软件内按下 `Ctrl+V`，监听器会识别剪贴板内是否包含图像 (Blob)。
* **写入磁盘**：包含图像时，前端将图片转化为 `Uint8Array` 字节流传给 Rust，Rust 将其硬写入工作区下隐藏的 `.assets` 文件夹，并返回物理绝对路径。
* **越狱渲染**：当 Markdown 遇到如 `![截图](C:\...png)` 的本地路径时，浏览器默认禁止渲染。系统通过正则匹配拦截该 IMG 标签，触发自定义的 `window.loadLocalImage` 钩子。该钩子调用 Rust 的 `read_raw_file` 读取本地图片的字节流，在前端拼装成合法的 Blob URL 喂给 `img.src`，实现完美的高清渲染。

#### 3.6.3 图片尺寸控制与全屏预览

* **指定宽度**：支持 Obsidian 等变体语法。用户编写 `![图片描述|300](路径)` 时，图片会被强制渲染为 300px 宽度。
* **沉浸式双击全屏**：无论在视觉模式还是预览模式，只需双击或点击任意图片，即刻弹出一个全屏幕、纯黑底色、无边框的图片查看器（支持高清放大查看），点击背景任意处关闭。

### 3.7 上下文提纯与格式剥离 (AI Purify)

* **需求场景**：将大模型生成的带有复杂排版的 Markdown 丢回给大模型时，过多的 `*` 和 `#` 会干扰。
* **临时提纯复制**：点击工具栏的“导出/提纯”下拉菜单，弹出只读的提纯 Modal。
* **正则剥离**：底层运行强力的正则表达式，不仅去除了 HTML 标签，还清除了所有 Markdown 标识符、表格线、链接标记等，只保留最纯粹的汉字和英文。
* 弹窗内提供“一键复制”按钮，带“✅ 已复制”的成功动效。

### 3.8 知识图谱初步：双向链接 (Bi-directional Links)

* **语法绑定**：识别 `[[目标笔记名]]`。
* **自动映射**：在视觉模式下，该语法被渲染为带有蓝色下划线的可点击实体标签。
* **新建或跳转**：点击标签，若系统内存有该同名文件，直接打开并切入新 Tab；若没有，在后台静默建立新笔记。
* **反向链接追踪 (Backlinks)**：编辑器底部面板会实时高强度检索所有笔记的内容。如果发现其他笔记的正文中包含了 `[[当前笔记名]]`，则在面板中以瀑布流形式列出引用来源及摘要上下文。

---

## 第四部分：设置与底层配置 (Settings & Config)

### 4.1 全局工作区设置 (Workspace Config)

* 左下角常驻“工作区设置”按钮。
* 弹窗允许用户配置底层存储的根目录物理绝对路径（如 `D:\MyNotes`）。
* 保存后，软件热重载，并强制 Rust 引擎去新路径下重新扫描或初始化 `supertxt_index.json`。

### 4.2 独立文件系统探测

* 每个笔记卡片内部均显示其绝对路径地址。
* 用户点击该路径时，触发后端的 `open_folder` 指令。
* Windows 平台下，Rust 会通过 `explorer /select, 路径` 命令，直接在系统资源管理器中打开该文件夹，并**高亮选中**该文件，彻底打通软件与操作系统的隔阂。

---

## 第五部分：非功能性与性能需求 (NFR)

### 5.1 性能指标 (Performance)

* **内存管理**：得益于 Tauri 底座，软件在 Windows 后台驻留时，内存占用需稳定在 50MB - 100MB 之间。
* **长列表渲染**：当笔记数量达到 1000+ 级别时，依赖 React 的 `useMemo` 对搜索过滤进行严格的计算隔离，避免因输入框文字变动引发全局重绘掉帧。

### 5.2 安全与稳定性保障 (Security & Stability)

* **防死锁递归**：所有涉及获取全量子目录（`getSubCategoryIds`）的方法已加入安全检查，即使底层索引遭到外部破坏形成死循环嵌套，系统也能及时跳出。
* **错误熔断**：React 生命周期内包裹的 `<ErrorBoundary>` 必须在检测到致命错误时拦截崩溃，并在屏幕输出带有堆栈追踪信息 (`componentStack`) 的红色故障面板，以便开发者快速定位（特别是升级迭代时可能出现的 `undefined` 引用错位）。

---
